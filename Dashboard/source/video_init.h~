// Standard C includes
#include <stdio.h>

// Xenon includes
#include <xenos/xenos.h>
#include <console/console.h>
#include <usb/usbmain.h>

// Do all inits
void mainInit(){	
	xenos_init(VIDEO_MODE_AUTO);
	console_init();

	usb_init();
	usb_do_poll();
	kmem_init();
}
